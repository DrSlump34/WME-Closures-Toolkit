# Contrôles et tests

Scripts Node, sans dépendance. Ils lisent **le fichier réel** (`../WME_ClosuresToolkit.user.js`)
plutôt qu'une copie : un test qui s'exécute sur autre chose que le code livré ne prouve rien.

```bash
cd tools
node check-keys.js        # avant toute publication
```

## Avant de publier

| Script | Ce qu'il vérifie | Pourquoi il existe |
|---|---|---|
| `check-keys.js` | Les 8 langues ont les **mêmes clés, types et arités** ; les nouvelles clés rendent du texte | Un argument oublié affiche un trou à l'écran, sans erreur |
| `check-help.js` | L'aide se rend dans les 8 langues, même nombre de sections, aucune vide, et **section par section** : laquelle retombe sur l'anglais, pour quelles langues | `_L` retombe **silencieusement** sur l'anglais : une langue oubliée ne lève rien. ⚠️⚠️ **Jusqu'au 08/08/2026 ce contrôle ne regardait que h14** et annonçait « AIDE COMPLÈTE DANS LES 8 LANGUES » pendant que **dix sections sur onze** rendaient de l'anglais à l'italien et à l'hébreu. Il mesure désormais chaque section, la dette restante est **nommée dans le fichier et affichée à chaque exécution**, et le verdict final ne dit plus « complète » quand elle ne l'est pas. Une section réparée qui reste dans la liste de dette fait **échouer** le contrôle, pour que le chiffre ne mente jamais par excès. ⚠️⚠️ **Réparé, il mentait encore de deux façons, découvertes le 11/08/2026** : il comparait une **fenêtre fixe de 900 caractères**, qui débordait sur la section suivante dès que la section était courte — et le titre de cette suivante, toujours traduit puisqu'il vient de `t()` et non de `_L()`, suffisait à masquer le repli (h5, 390 caractères, était annoncée traduite sans avoir jamais eu ni `it` ni `he`) ; et comparer des **rendus** ne suffit pas, puisqu'une seule interpolation traduite dans une section par ailleurs anglaise fait différer le rendu (h10 porte un `${t('shpNetworkHelp')}`). Il croise désormais **deux lectures** — les langues déclarées dans le `_L` du source, et le rendu — et **affiche tout désaccord** entre elles au lieu de le taire : c'est ce désaccord qui a révélé les deux mensonges. Vérifie aussi l'**équilibre des balises** dans les 8 langues : un `</td>` oublié ne fait échouer aucun autre contrôle et disloque le tableau chez le seul éditeur qui lit cette langue-là |
| `check-lib-copie.js` | La copie de `WMEPrefs` embarquée est **identique** à `../../WME-Prefs/WMEPrefs.js`, et fonctionne | Deux copies qui divergent en silence sont le pire des deux mondes |
| `check-contraste.js` | Les couleurs **du thème** respectent 4,5:1 (WCAG), thème clair **et** compact | L'audit du 01/08 a mesuré 41 paires en échec sur 90. Le thème compact repeignait le fond en gris Win95 sans redéfinir les textes secondaires : 2,21:1 et **1,47:1**, quasi invisible. On juge les couleurs à l'œil, et l'œil s'habitue |
| `check-bilan-defile.js` | Le **bilan d'application déplié** n'est pas rogné par son parent, et il ne pousse pas les boutons du bas hors de l'overlay. ⚠️ Besoin de **Chrome** (il mesure des pixels) | Signalé par l'auteur le jour de la 1.11.00 : « on ne voit que les 5 premières lignes ». Le corps portait pourtant `max-height` **et** `overflow-y:auto` — le défaut était dans la **chaîne de conteneurs** : `#wct-overlay` est un flex column borné, le bilan y est comprimé quand la place manque, et son `overflow:hidden` coupait le bas de la zone défilante, **barre comprise**. ⚠️⚠️ **La leçon est dans l'instrument autant que dans le défaut** : la première version de cette mesure lisait `corps.clientHeight` et répondait « défilable partout » — elle mesurait ce que le corps *croit* afficher, pas ce que son parent en laisse voir, et déclarait sain exactement le défaut qu'on lui demandait de trouver. Porte un **témoin** (les trois pièces du correctif annulées) et un **témoin de non-régression** : un bilan devenu incompressible pourrait pousser Appliquer/Vider sous le pli — ce serait déplacer le défaut, pas le corriger |
| `check-onglets.js` | La barre des **sous-onglets de Configurer** tient sur **une seule ligne** dans les 8 langues, sur 4 tailles de fenêtre. ⚠️ Seul outil qui a besoin de **Chrome** : il mesure des pixels, pas du texte. S'il ne le trouve pas, il le dit et sort en erreur | Le 3ᵉ onglet de la 1.10.00 (« En continu ») a fait passer l'allemand à **296 px dans une colonne de 296** : zéro marge. Ça ne débordait pas ce jour-là, sur cette machine, avec cette police de repli — ça aurait débordé chez le premier éditeur dont la police rend deux pixels plus large. Les trois défauts partis en production avec la 1.07.01 étaient tous de rendu, tous invisibles aux 186 tests. ⚠️ Il compte les **lignes**, pas le débordement : depuis que `.wct-tabs` porte `flex-wrap`, un libellé trop large ne déborde plus, il passe à la ligne — surveiller le débordement seul serait surveiller une chose qui ne peut plus arriver. Porte un **témoin** (une barre volontairement trop large) qui doit être signalé dans les 4 configurations |
| `check-rtl.js` | Le panneau **se retourne vraiment en hébreu** : ancrage, interrupteur, les deux popovers et la barre d'attente sont mesurés en LTR puis en RTL, avec Chrome. ⚠️ Chaque fragment est remonté **sous son vrai parent** et les styles **inline** des popovers sont extraits du fichier, jamais recopiés | L'hébreu est livré depuis la 1.07.01 et **personne n'a jamais vu l'écran**. Le bloc CSS `[dir="rtl"]` avait lui aussi été écrit sans être vu : sur ses 7 règles, **une seule mordait**. Les 4 autres échouaient pour trois raisons différentes, toutes invisibles à la lecture — un sélecteur visant `#wct-overlay` alors que l'interrupteur vit dans `#wct-sidebar` ; un `right` en **style inline**, qu'aucun sélecteur ne peut battre ; une animation portant sur `left`, propriété **ignorée en RTL** quand `right` et `width` sont posés, ce qui laissait la barre d'attente parfaitement immobile. ⚠️ Le critère n'est pas « la géométrie change » : le popup des préréglages changeait bel et bien — il passait de 268 à 592 px, `left` et `right` s'appliquant tous les deux. On mesure donc **même largeur + ancrage inversé + reste dans le panneau**. Deux témoins : un **témoin de moteur** qui fait constater à Chrome laquelle des deux propriétés il ignore selon le sens (au lieu de citer la norme), et un **témoin de correctif** qui rejoue tout sans le bloc RTL — une pièce saine doit alors retomber en français, sauf si elle tient à des **propriétés logiques**, auquel cas elle se retourne seule et le dit |
| `check-entete.js` | La barre de l'**en-tête** tient sur une ligne dans les 8 langues, sur 4 tailles de fenêtre, **avec et sans la pastille de mise à jour**. ⚠️ Besoin de **Chrome** (il mesure des pixels) | La pastille de la 1.13.02 fait un **quatrième** bouton dans une ligne qui porte déjà le nom du script, le numéro de version et le compteur de file — et ce compteur est traduit : l'allemand écrit « 999 Einträge in der Warteschlange ». Mesuré : en fenêtre de 540 px, l'allemand passait le titre **sur deux lignes**, alors qu'il tenait à **1 px près** sans la pastille. ⭐ **Ce qu'on ajoute se paie sur ce qui était là**, et le vrai résultat de ce contrôle n'est pas le verdict mais l'**écart entre les deux colonnes** : chaque langue est mesurée deux fois, avec la pastille et sans, et un défaut déjà présent sans elle est **annoncé comme tel** plutôt que mis à son compte. ⚠️⚠️ **La mesure s'est trompée deux fois avant de mordre**, et les deux erreurs valent d'être connues : (1) surveiller le débordement ne sert à rien — `.wct-hdr-title` est une boîte flex **compressible**, elle ne déborde jamais, elle se replie, et `scrollWidth` reste sage ; le témoin est passé pour sain dans les 4 configurations. (2) Compter les lignes par un `Range` sur le **conteneur** annonçait « 2 lignes » pour les 8 langues, y compris en plein écran : le titre est un flex `align-items:center` dont le texte (14 px) et les deux spans (12 px) n'ont pas le même `top` **alors qu'ils sont sur la même ligne**. On mesure donc chaque enfant séparément. ⚠️ Depuis que le titre est en `nowrap` + `overflow:hidden`, le repli **ne peut plus se produire** : le compteur de lignes est devenu un garde-fou et l'indicateur est la **troncature** — le compteur de file tronqué est voulu, le numéro de version rogné est un défaut. Le témoin a dû suivre ce changement, sans quoi il ne mordait plus |
| `check-emoji.js` | Tout caractère à `Emoji_Presentation=false` **affiché** est suivi de `U+FE0F` | Sans ce sélecteur, le navigateur rend un **glyphe texte monochrome**, fin et pâle. Corrigé pour un seul caractère en 0.81.00 (l'info, mesurée à 4,2 px) ; l'audit du 01/08 en a trouvé **402 autres**, dont toute la barre d'action basse. Aucun test ne le regardait. Ignore les commentaires et comprend les deux écritures du fichier (caractère réel ou `\uXXXX`) |
| `check-ancrage.js` | La fenêtre du script **tient dans l'écran** et reste **réglable** : le bornage de la géométrie mémorisée (partie A, Node pur, extraite de `_ovClamp`) et la position que le CSS produit vraiment, à 4 tailles de fenêtre (partie B, ⚠️ besoin de **Chrome**) | MisterLogik, 27/08/2026 : « le réglage de la fenête n'est pas / plus possible ? Elle est attaché en bas... » — et GNico73, **même version, autre écran** : « la fenêtre est bien flottante ». Deux constats opposés sur le même code : une mesure à faire, pas un avis à rendre. Elle a donné le chiffre — **6 px du bas** sous 680 px de hauteur, contre **48 px** en plein écran : « attaché en bas » n'était pas un bug mais ce que les media queries demandent, et rien ne permettait de rétrécir. ⚠️ Le vrai risque est du côté du **correctif**, pas du défaut : une géométrie mémorisée sur grand écran, relue sur un portable, mettrait le panneau hors champ — et il est en `position:fixed`, donc **plus rien ne permettrait d'aller le rechercher**. Le bornage **à la relecture** est la seule chose qui sépare « la fenêtre se souvient » de « la fenêtre a disparu ». ⭐ Les deux premières versions de ce contrôle ont échoué et **c'est le contrôle qui avait tort** : j'attendais une position en haut à gauche et une hauteur au plancher là où la mesure rendait `y=16, h=224` — un plancher n'est pas une cible. Une troisième assertion tombait sur la **bordure de 1 px** du panneau, que `getBoundingClientRect` compte et que `right:0` ignore. ⭐ **Partie C, ajoutée le 29/08/2026** : un panneau **replié** doit retomber sur la hauteur de son **contenu**, même quand une géométrie est mémorisée. Glenan56 a livré une capture de panneau vide — il n'était pas cassé, il était replié, et depuis que `_ovApply` pose `height` en style **inline**, replier ne rétrécit plus rien : il reste un rectangle de **583 px de vide**, mesuré, qui se lit comme une panne. Le critère est un **rapport** (hauteur repliée avec géométrie = hauteur repliée sans), jamais un seuil en pixels qui se périmerait à la première ligne ajoutée dans l'en-tête. Témoin : la règle CSS retirée de la feuille d'essai, et le vide doit revenir |

## Tests unitaires

| Script | Couvre |
|---|---|
| `test-poly.js` | Moteur géométrique de la zone : point-dans-polygone, fraction >50 %, concave, trous, 50 % pile non retenu, normalisation du tracé |
| `test-export.js` | Formats WKT et KML **extraits du fichier réel** : ordre lon/lat, anneaux, trous, échappement XML |
| `test-roundtrip.js` | **Aller-retour** export → import : ce qu'on écrit, on doit savoir le relire. C'est ce test qui avait révélé que WCT ne relisait pas son propre CSV |
| `test-imp-detect.js` | Détecteur de l'onglet Import, surtout les cas **ambigus** : `.json` GeoJSON ou préréglages, `.kml` tracé ou zone, fichier mixte |
| `test-bilan.js` | L'**état d'application** posé sur chaque carte de file, et la structure du **bilan** de fin. ⚠️ Vérifie surtout deux invariants de sens : « échec » est réservé au cas où **rien** n'a été posé (sur un lot à moitié écrit, il enverrait tout refaire et l'éditeur poserait des doublons sur la carte), et un **manque** — segment hors du modèle — n'est pas une **erreur** du serveur. Vérifie aussi que le bouton **« Continuer »** de la pause entre lots n'est plus dans le journal : il y était, ce qui faisait du journal un *contrôle*, et le replier aurait figé l'application en attente d'un clic devenu invisible. Porte un **témoin** : la règle doit rendre trois valeurs distinctes, sans quoi une règle qui répond toujours pareil passerait tous les cas |
| `test-centrage.js` | Recadrage **sur ce qui reste visible** (hors volet WME, hors panneau, hors barre du bas) : le point doit atterrir au centre de la partie visible, au **pixel près et au zoom d'arrivée**. Vérifie aussi que le niveau de zoom se calcule sur la surface visible et non sur le canevas entier |
| `test-pave.js` | Découpage d'une zone en **lots** : un lot = un recadrage de carte et une entrée de file, donc le nombre de lots est ce que l'éditeur paie. Vérifie la règle « ce qui tient dans une vue ne fait qu'un lot » sur 200 dispositions, plus les bbox, les doublons et les cas dégénérés |
| `test-raccourcis.js` | La table des **raccourcis clavier** et ses règles de déclenchement, sur un DOM simulé. Verrouille deux garanties invisibles à la relecture : ⚠️ **aucun raccourci ne déclenche « Appliquer » ni « Vider »** (les deux seules actions irréversibles), et une frappe **dans un champ** ne déclenche jamais rien. Vérifie aussi qu'un raccourci d'onglet ne vaut que dans son onglet, et qu'un bouton grisé ne répond pas au clavier |
| `test-queue-total.js` | Le nombre de fermetures que la file va **réellement écrire** (`_queueTotalClosures`), extrait du fichier réel. Ce compte alimente la **seule barrière avant écriture** : jusqu'à la 1.02.00 la confirmation annonçait un nombre de *lots* (« Appliquer 3 lot(s) ? ») alors qu'une zone de 1 150 segments sur 21 occurrences en écrit **24 150** — et rien ne sait les défaire. Couvre les lignes supprimées à la main, les segments défaillants, et les entrées virages (dont `segIds` est vide) |
| `test-plage.js` | Les **bornes de la plage de dates** dans `buildClosureList`, extraite du fichier réel. C'est elle qui décide de ce qui sera **écrit dans WME**, et aucun test ne la regardait. La borne portait sur la **fin** de l'occurrence : toute fermeture passant minuit faisait donc perdre le **dernier jour de la plage, en silence** — « du 1er au 31 août, 21h→5h » ne posait que 30 nuits. Elle porte désormais sur le **début**, comme le filtre des jours de la semaine, celui des jours fériés et la boucle des jours calendaires, qui raisonnaient déjà tous sur le début. Vérifie que le dernier jour produit sa fermeture, que le débordement est **annoncé** et non corrigé en douce, et — **témoin** — qu'aucune annonce n'apparaît quand rien ne déborde : un avertissement permanent est un avertissement qu'on cesse de lire. Verrouille aussi que la fin de plage est **minuit LOCAL** : construite depuis `new JDate('AAAA-MM-JJ')`, que JS parse en UTC, elle tombait à 02:00 du matin en France l'été et laissait passer des départs hors plage. Couvre les deux onglets, les deux modes horaires et `+Jours`. Enfin il **croise deux calculs qui vivent à 6 000 lignes l'un de l'autre** : le nombre d'occurrences annoncé par l'avertissement « Répéter » doit être celui que la génération produit — c'est ce croisement qui a révélé la borne en UTC |
| `test-maj.js` | Ce qui décide d'allumer la **pastille de mise à jour** : `_majCmp` et le motif qui lit le `@version` du `.meta.js` GreasyFork, **extraits du fichier réel**. La pastille envoie l'éditeur réinstaller le script — deux pièces la commandent, et aucun autre outil ne les regarde. ⚠️ Le piège est la comparaison de **chaînes** : elle fait de `1.9.00` une version plus récente que `1.13.01`, et le script a publié huit versions en `1.9.xx`. Dans ce sens-là la pastille reste **éteinte** alors qu'une version est sortie — une panne qui ne se signale pas. **Témoin de morsure** : les mêmes cas sont rejoués avec la comparaison naïve et doivent tomber, sinon le « tout passe » ne distingue pas `_majCmp` d'une version fausse. Couvre aussi les zéros omis (`1.14` = `1.14.00`), les formats refusés (version locale à `?`, suffixe de pré-publication, page HTML rendue au lieu du `.meta.js`) et un `@version` cité dans une description, qui ne doit pas être lu à la place du vrai |
| `test-imp-route.js` | Ce qui se passe **après** la détection : le libellé du type reconnu, le filet à exceptions du point d'entrée, l'import de zone qui doit lire le fichier entier. Et surtout : **aucune portée ne masque la fonction de traduction `t()`** |
| `test-geojson-noms.js` | Un tracé importé porte-t-il un **nom**, et une **couleur qui veut dire quelque chose** ? Le champ de nom (`_traceNameField`, partagé avec le Shapefile), la règle d'attribution des couleurs (`_traceAssignColors`) et `parseGeoJSON`, joués sur les **trois GeoJSON réels** du dossier `Exemples`. ⚠️ `Exemples/` est hors dépôt (`.gitignore`) : sans ces fichiers le contrôle **annonce la mesure non faite** et sort en 2, il ne se tait pas | MisterLogik, 27/08/2026 : « Tu sembles avoir géré des couleurs des tracés, mais pas les noms ». **Les deux moitiés étaient à corriger, et pas dans le sens qu'on croit.** Les noms : `parseGeoJSON` ne cherchait que `name | Name | nom | title | label`, et le GeoJSON du Triathlon de Dinard nomme ses 28 tracés par `epreuve` — d'où « TDCE2026-parcours (1/28) » … « (28/28) », vingt-huit lignes indiscernables. Les couleurs : elles ne venaient **pas** du fichier, qui n'en contient aucune — c'était le roulement sur 8 teintes, tiré 28 fois. 🔴 **Le contrôle a trouvé une régression que le correctif créait** : sur `parcours para marathon`, les 21 lignes portent la même valeur sur **tous** leurs champs (`evenement` = « JO 2024 » vingt et une fois) — regroupées, elles devenaient monochromes, là où le roulement en donnait huit et laissait suivre les tronçons à l'œil. D'où la garde : **on ne regroupe que si le regroupement sépare**. Elle porte son témoin de morsure, la règle rejouée sans elle |
| `test-repli.js` | La règle qui décide qu'un panneau **replié sans que l'éditeur l'ait demandé** doit se rouvrir (`_repliSubiDoitCesser`), extraite du fichier réel. WCT se replie lui-même pour libérer la carte pendant un tracé de zone et compte sur le **retour** de `sdk.Map.drawPolygon()` pour déplier : si cette promesse ne rend jamais la main — tracé abandonné sans fermer le polygone — plus rien ne déplie, **jamais**. ⚠️ Ce que le filet ne doit **pas** faire compte autant : déplier au mauvais moment recouvre la carte que le repli venait de libérer, d'où un déclencheur qui est le **changement** de sélection et non sa présence. Vérifie aussi que le filet est **branché** — exiger `if(` collé à l'appel n'est pas du zèle : mesuré, un `if(false && …)` passait pour un filet en place. Témoin : la règle naïve « replié + sélection ⇒ déplier », qui doit rater 4 cas sur 7 |

## Le module d'extraction

`lib-dico.js` **est le seul endroit qui sait lire le dictionnaire dans le fichier réel.** `check-keys.js`,
`check-help.js`, `audit.js` et `audit-ortho.js` en portaient chacun sa copie — quatre copies du même
scanner, dans un dossier dont ce README explique qu'une copie ne prouve rien. Et ce scanner était faux
sur trois points, découverts en le cassant pour de vrai le 2026-08-01 :

- il **ne sautait pas les commentaires**, donc `indexOf` pouvait trouver un `const D = …` écrit dans un
  commentaire *avant* le vrai dictionnaire (arrivé : les trois outils sont tombés d'un coup) ;
- un **guillemet ASCII isolé** dans une chaîne à apostrophes le faisait avaler le reste du fichier. Le
  userscript en contenait cinq — des abréviations hébraïques du type `ק״מ` — qui tenaient par un
  appariement fortuit ; le sixième a tout cassé ;
- il **ignorait les regex littérales**, dont les guillemets et accolades faussaient le comptage : il
  annonçait `_epsgFromPrj : 3675 lignes` pour une fonction qui en fait **quatre**.

`test-lib-dico.js` (21 tests) rejoue ces trois pannes. Ne pas réintroduire de scanner local.

`poly-core.js`, `imp-detect.js` et `poly-pave.js` sont les modules testés. **Aucun n'est plus une copie :
tous les trois extraient leur code du fichier réel.**
`poly-core.js` a été converti en dernier (2026-08-01). Motif : c'était le dernier module encore copié,
donc les 28 tests du moteur géométrique validaient du code qui n'était pas celui qui tourne. Sa copie
**avait d'ailleurs déjà divergé** — elle nommait les fonctions `_polyRings` et `_polyBBox` là où le
fichier réel dit `_polyRingsOf` et `_polyBBoxOf` (corps encore identiques, noms non). Les anciens noms
restent exportés comme alias vers le code réel. Il extrait le bloc `POLY_INSIDE_FRAC` → fin de
`_polyInsideFrac`, **seuils compris** : un seuil changé dans le userscript est donc vu par les tests.
`imp-detect.js` **était** une copie lui aussi — il extrait désormais la fonction du fichier réel
(2026-07-26). Motif : le bug 0.97.01 vivait à deux lignes du code copié, dans une zone qu'aucun
test n'atteignait. Une copie ne prouve rien.
`poly-pave.js` extrait lui aussi (2026-07-30). Motif : le pavage était enfoui dans
`_polyLoadAndSelect`, qui parle au SDK et déplace la carte — donc intestable, et personne n'a vu
qu'une zone tenant dans un écran ressortait en trois lots. Il en a été sorti en fonction pure.
`centrage.js` extrait `_decalageVisible` et `_zoomPourTaille` (2026-07-30). La **mesure** des
rectangles (volet WME, panneau, barre du bas) ne se teste pas hors navigateur — mais le **calcul**,
lui, décide où la carte atterrit : c'est celui-là qui posait 620 px en dur et ignorait le volet.

⚠️ **Le piège du masquage de `t()`** — la fonction de traduction est une variable globale nommée
`t`. Une fonction qui prend un paramètre `t` (ou déclare un `const t`) **et** appelle `t('clé')`
dans son corps lève un `TypeError` à l'exécution seulement, jamais à `node --check`. C'est ce qui a
rendu l'onglet Import totalement muet en 0.97.00 : l'exception partait dans une promesse que
personne n'attendait. `test-imp-route.js` balaie tout le fichier pour ça.

## Audit

| Script | Cherche |
|---|---|
| `audit.js` | Interpolations HTML non échappées, clés i18n orphelines ou manquantes, ids dupliqués, `catch` muets, `setInterval`, restes de mise au point |
| `audit-catch.js` | Classe les `catch` vides **par risque** : avaler l'échec d'une écriture n'est pas avaler celui d'un cadrage optionnel |
| `audit-ortho.js` | Orthographe des **textes affichés** (pas des commentaires) : accents, doubles espaces, apostrophes droites, ponctuation française |
| `audit-tooltips.js` | Éléments interactifs sans `title`. Règle du projet : aucun `button`, `select` ou `textarea` sans infobulle |

⚠️ `audit.js` signale des interpolations HTML « suspectes » qui sont, pour la plupart, **échappées
en amont** (`descHtml`, `nameHtml`…). Il désigne des endroits à relire, pas des failles : vérifier
d'où vient la donnée avant de conclure.

## Après toute édition du userscript

```bash
node --check ../WME_ClosuresToolkit.user.js
git diff --numstat        # plus de lignes modifiées que demandé = corruption
```

⚠️ **Ne jamais éditer le userscript via PowerShell `Set-Content`** : il ajoute un BOM (et peut
casser l'UTF-8). Un BOM en tête menace le parsing du bloc `// ==UserScript==`. Et `Get-Content -Raw
-Encoding UTF8` **ne peut pas le détecter**, puisqu'il le retire à la lecture — seul `git diff` le
montre. Éditer avec l'outil d'édition, ou `node fs.readFileSync/writeFileSync(..., 'utf8')`.
